.DS_Store
.gradle/
.idea/
.kotlin/
build/
local.properties

# Release-Signing: NIEMALS committen (Keystore + Klartext-Passwort).
*.jks
*.keystore
keystore.properties

# Persönliche Notizen mit Zugangsdaten — gehören nur auf den eigenen Rechner.
Gitignore - wichtige Passw*
